<div class="dashboard-container">
  <div class="dashboard-card">
    <div class="dashboard-header">
      <div>
        <h1>Dashboard</h1>
      </div>
      <div class="user-info">
        <span>Olá, <span class="username"><%= current_user %></span>!</span>
        <%= button_to "Sair", logout_path, method: :delete, class: "btn-logout" %>
      </div>
    </div>

    <% if flash[:success] %>
      <div class="alert alert-success">
        <%= flash[:success] %>
      </div>
    <% end %>

    <div class="dashboard-content">
      <p class="welcome-message">
        Bem-vindo ao sistema! Você está autenticado com sucesso.
      </p>

      <p>
        <strong>Informações da sessão:</strong>
      </p>
      <ul>
        <li>Usuário: <strong><%= current_user %></strong></li>
        <li>Status: <strong>Conectado</strong></li>
        <li>Sessão ativa: <strong>Sim</strong></li>
      </ul>

      <p style="margin-top: 30px; padding-top: 20px; border-top: 1px solid #e2e8f0;">
        Esta é uma página protegida que só pode ser acessada por usuários autenticados.
        Se você tentar acessar esta página sem estar logado, será redirecionado
        automaticamente para a tela de login.
      </p>
    </div>
  </div>
</div>
